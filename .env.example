# Optional: forward booking-form submissions to email/Slack/CRM.
# When unset, the /api/booking endpoint validates and logs the request
# (the form also offers a WhatsApp hand-off, so leads are never lost).
BOOKING_WEBHOOK_URL=
